var liveStr1 = 'Darth (Bane) dark (lord of (the) sith)';
var liveStr2 = 'N(0(p)3';
var liveStr3 = 'soup ) table (fark)'

function validParens(str) {
	var open = 0;
	var close = 0;
	for (var idx = 0; idx < str.length; idx++) {
		if (str[idx] == '(') {
			
		};
	};				
};
console.log(validParens(liveStr3));



// return true for all valid sets of parens
// return false for all incomplete sets

// check - every '(' must have matching ')', vice versa




